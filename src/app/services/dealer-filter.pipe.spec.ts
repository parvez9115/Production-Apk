import { DealerFilterPipe } from './dealer-filter.pipe';

describe('DealerFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new DealerFilterPipe();
    expect(pipe).toBeTruthy();
  });
});